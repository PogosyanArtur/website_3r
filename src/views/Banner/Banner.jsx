import React from 'react'

import styles from './styels.module.scss'

const Banner = () => {
  return (
    <main className={`${styles.Banner}`} id='home'>
      <h1 className={`${styles.Title}`}>лидеры в области асфальтобетоной крошки</h1>
    </main>
  )
}

export default Banner
